from app.core.database import get_db

__all__ = ["get_db"]

# Authentication/authorization dependencies (get_current_user, require_role)
# are added in Phase 3 once the User model and auth service exist.
